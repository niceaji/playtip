---
import Item from "./Item.astro";

const { tags } = Astro.props;
---

<h2 class="mt-10 mb-1">관련 TIP</h2>
<div class="grid grid-cols-1 gap-3 md:grid-cols-2">
  {tags.map((tag: string) => <Item title={`#${tag}`} url={`/tags/${tag}`} />)}
</div>
