---
layout: ~/layouts/CodeLayout.astro
title: 배열 순환-map()
description: 각각에 대하여 주어진 함수를 호출한 결과를 모아 새로운 배열을 반환
isJS: true
---

## js

```js
const words = ["js", "html", "css"];
const titles = words.map((word) => `<h1>${word}</h1>`);

console.log(titles);
console.log(words);
```
