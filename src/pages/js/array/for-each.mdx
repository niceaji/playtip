---
layout: "~/layouts/CodeLayout.astro"
title: "배열 순환-forEach()"
description: "주어진 함수를 배열 요소 각각에 대해 실행"
isJS: true
---

## js

```js
const words = ["js", "html", "css"];

const result = words.forEach((word) => console.log(word));

console.log(result);
```
