---
layout: ~/layouts/CodeLayout.astro
title: 배열을 문자열로 만듦-join()
description: 배열의 모든 요소를 연결해 하나의 문자열로 만듦
isJS: true
tags:
  - array.join
---

## js

```js
const words = ["html", "js", "css"];

const str1 = words.join("\n");
const str2 = words.join(" ");

console.log(str1);
console.log(str2);
```
