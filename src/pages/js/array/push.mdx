---
layout: "~/layouts/CodeLayout.astro"
title: "배열에 요소 추가-push()"
seoTitle: "Add Element To Array"
description: ""
isJS: true
---

## js

```js
function repeat(word, count) {
  const result = [];
  for (let i = 0; i < count; i++) {
    result.push(word);
  }
  return result;
}

const result = repeat("tip", 3);

console.log(result);
```
